/** Mặc định khi chọn Custom REST — cùng endpoint với DictionaryAPI.dev (GET JSON). */
export const DEFAULT_DICTIONARY_CUSTOM_URL_TEMPLATE =
  'https://api.dictionaryapi.dev/api/v2/entries/en/{{word}}'
